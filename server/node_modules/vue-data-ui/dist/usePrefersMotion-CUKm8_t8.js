import { ref as t, onMounted as r, onBeforeUnmount as d } from "vue";
function u() {
  const o = t(!1);
  return r(() => {
    const e = window.matchMedia("(prefers-reduced-motion: reduce)"), n = () => {
      o.value = e.matches;
    };
    n(), e.addEventListener("change", n), d(() => {
      e.removeEventListener("change", n);
    });
  }), o;
}
export {
  u
};
